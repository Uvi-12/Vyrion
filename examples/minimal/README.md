# Minimal example

```
pip install vyrion
python app.py
```

`app.py` mints a genuine Seal for a transfer, verifies it (passes), then tries a forged
approval and a rebound one, and shows both rejected.
